#include <stdio.h>

int main() {
    // 1. Duas variáveis inteiras
    int quantidadeAlunos;
    int quantidadeComputadores;

    // 2. Atribuir valores às variáveis
    quantidadeAlunos = 30;
    quantidadeComputadores = 20;

    // 3. Exibir os valores armazenados
    printf("Quantidade de alunos: %d\n", quantidadeAlunos);
    printf("Quantidade de computadores: %d\n", quantidadeComputadores);

    return 0;
}
