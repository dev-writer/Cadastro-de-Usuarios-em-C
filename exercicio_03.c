#include <stdio.h>

int main() {
    // 1. Duas variáveis inteiras
    int idade;
    int numeroFilhos;

    // 2. Atribuir valores às variáveis
    idade = 25;
    numeroFilhos = 2;

    // 3. Exibir todas as informações na tela
    printf("Idade: %d\n", idade);
    printf("Numero de filhos: %d\n", numeroFilhos);

    return 0;
}
